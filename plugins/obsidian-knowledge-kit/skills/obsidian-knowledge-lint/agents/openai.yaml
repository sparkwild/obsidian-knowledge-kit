interface:
  display_name: "Vaultwright Lint"
  short_description: "Check knowledge quality inside an Obsidian vault"
  default_prompt: "Run the Vaultwright knowledge lint workflow against the active Obsidian vault, keep it read-only by default, and report broken links, missing sources, stale concepts, orphan notes, and weak log trails."
