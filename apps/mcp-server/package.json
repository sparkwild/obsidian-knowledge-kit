{
	"name": "obsidian-obswiki-mcp-server",
	"version": "0.1.0",
	"description": "Agent-first MCP server for Obswiki: read-only default, bounded writes, and review-gated apply.",
	"private": true,
	"type": "commonjs",
	"main": "dist/server.js",
	"types": "dist/server.d.ts",
	"scripts": {
		"test": "node ./scripts/smoke.mjs",
		"smoke": "node ./scripts/smoke.mjs",
		"typecheck": "tsc --noEmit -p tsconfig.json",
		"build": "tsc -p tsconfig.json",
		"start": "node dist/server.js"
	},
	"dependencies": {
		"@obswiki/core": "file:../../packages/core"
	},
	"devDependencies": {
		"@types/node": "^22.14.0",
		"typescript": "^5.8.2"
	},
	"license": "MIT"
}
